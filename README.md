# TelemetryGuard
TelemetryGuard is a PowerShell tool that enforces WER‑style privacy across Windows, Office, and Visual Studio.  It minimizes telemetry to crash/error reporting, licensing checks, and update readiness only, with edition‑aware  controls and clear logging for auditability.
